# EOPP — Чеклист задач (02.06.2026)

## Агент 1: Browser Extension — UI и авторизация

### 1.1 Рефакторинг авторизационного окна (AuthGate)
- [ ] 1.1.1 Добавить кнопку закрытия/отмены в модалку авторизации (AuthGate), позволяющую выйти без ввода ключа
- [ ] 1.1.2 Скрыть содержимое основной модалки (Modal) до успешной авторизации — AuthGate должен полностью перекрывать UI
- [ ] 1.1.3 Реализовать кнопку «Выйти» / сменить API-ключ в заголовке модалки после успешной авторизации

### 1.2 Рефакторинг модалки и планировщика
- [ ] 1.2.1 Восстановить функциональность запланированного запуска по таймеру: гарантировать вызов `run()` при достижении целевого времени
- [ ] 1.2.2 Изменить пресеты времени в Scheduler с `10:00:00.0` / `12:00:00.0` на `10:00:00.5` / `12:00:00.5` соответственно
- [ ] 1.2.3 Удалить строку «Осталось:» из UI планировщика; оставить только заголовок «Запуск через» с таймером обратного отсчёта

---

## Агент 2: React Frontend — Фронтенд сервера

### 2.1 Вкладка «История»
- [ ] 2.1.1 Исправить ошибку 401 (Unauthorized) при запросе `GET /usage-log` — убедиться, что `api_key` передаётся в query-параметре или header

### 2.2 Стили вкладки «Капчи»
- [ ] 2.2.1 Исправить стили кнопок-табов (`.captcha-tab`, `.captcha-tab-active`) — обеспечить корректную визуальную индикацию активной вкладки

### 2.3 Idle-состояние вкладки капчи
- [ ] 2.3.1 Реализовать интерактивное idle-состояние для вкладки «Капчи» при отсутствии активных капч: анимированный спиннер или динамический текст «Ожидание запросов»

---

## Агент 3: Backend — Python сервер

### 3.1 Админ-панель: Мониторинг стримов
- [ ] 3.1.1 Добавить страницу в админке со списком подключённых клиентов SSE-стримов: api_key_id, timestamp подключения, IP-адрес

### 3.2 Админ-панель: Статистика тесткейсов
- [ ] 3.2.1 Отображать количество неразмеченных тесткейсов (`tests/test_cases/no_valid/`) и размеченных (`tests/test_cases/valid/`)
- [ ] 3.2.2 Отображать результаты последнего запуска бенчмарка: процент покрытия капч (success rate)

### 3.3 Trigger-test с привязкой к api_key
- [ ] 3.3.1 Модифицировать `POST /trigger-test` так, чтобы тестовые капчи отправлялись под api_key вызвавшего клиента (не под `__test_key__`)

### 3.4 Обязательная авторизация SSE-стрима
- [ ] 3.4.1 Запретить подключение к `GET /stream` без валидного `api_key`
- [ ] 3.4.2 Проверить, что все фронтенд-клиенты (React, Extension) передают `api_key` при подключении к стриму

---

## Декомпозиция по агентам

| Агент | Область | Ключевые файлы |
|-------|---------|----------------|
| **Агент 1** | Browser Extension (Tasks 1-2) | `yandex-browser-plugin/src/components/AuthGate.tsx`, `Modal.tsx`, `Scheduler.tsx`, `hooks/useScheduler.ts`, `store.ts` |
| **Агент 2** | React Frontend (Task 3) | `frontend/src/components/UsageHistory.jsx`, `App.jsx`, `CaptchaGrid.jsx`, `main.css`, `hooks/useSSE.js` |
| **Агент 3** | Python Backend (Task 4) | `src/routes.py`, `src/utils.py`, `src/api_keys.py`, `frontend/src/AdminPage.jsx` |
